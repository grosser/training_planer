To RSVP for Training <%= link_to @training.title, @training %><br/>
<%= @training.start %><br/>
<br/>
<br/>

Please click <b><%= link_to "confirm", confirm_participation_url(UrlStore.encode("#{@person.id}-#{@training.id}")) %></b>
